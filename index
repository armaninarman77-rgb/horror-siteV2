<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>НЕ ОБОРАЧИВАЙСЯ</title>
  <style>
    * { box-sizing: border-box; }
    body {
      margin: 0;
      font-family: Arial, sans-serif;
      background: #000;
      color: #e6e6e6;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      overflow: hidden;
      transition: background-color 0.16s ease;
    }
    .wrap {
      width: 92%; max-width: 520px;
      background: rgba(0,0,0,.85);
      border-radius: 18px;
      padding: 22px;
      position: relative;
      box-shadow: 0 30px 80px rgba(0,0,0,.9);
      overflow: hidden;
    }
    .noise {
      position: absolute; inset: 0;
      pointer-events: none;
      background:
        repeating-linear-gradient(0deg, rgba(255,255,255,.03) 0 1px, transparent 1px 2px),
        repeating-linear-gradient(90deg, rgba(255,255,255,.03) 0 1px, transparent 1px 2px);
      mix-blend-mode: overlay;
      animation: flicker 2.2s infinite;
    }
    @keyframes flicker {
      0%,100% { opacity: .12 }
      50% { opacity: .25 }
    }
    h1 { letter-spacing: 3px; text-align: center; margin: 4px 0; }
    .sub { opacity: .8; text-align: center; }
    .screen {
      margin: 14px 0; padding: 14px;
      min-height: 140px;
      border-radius: 12px;
      background: #050505;
      border: 1px solid #1f1f1f;
      font-family: monospace; line-height: 1.35;
      position: relative;
      overflow: hidden;
      position: relative;
    }
    .blink::after {
      content: '▌';
      animation: blink 1s steps(1) infinite;
    }
    @keyframes blink {
      50% { opacity: 0 }
    }
    .actions {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 10px;
      margin-top: 16px;
    }
    button {
      background: #0b0b0b;
      color: #e6e6e6;
      border: 1px solid #2a2a2a;
      border-radius: 12px;
      padding: 12px;
      font-weight: 800;
      cursor: pointer;
      outline: none;
      transition: all .3s ease;
    }
    button:hover {
      background: #111;
      transform: translateY(-1px);
    }
    button:active { transform: translateY(0); }
    .danger { border-color: #7a1111; color: #ffb3b3; }
    .safe { border-color: #145c2a; color: #b9ffd5; }
    .timer {
      margin-top: 10px;
      text-align: center;
      font-size: 14px;
      opacity: .8;
    }
    .overlay {
      position: fixed;
      inset: 0;
      background: #000;
      display: none;
      align-items: center;
      justify-content: center;
      z-index: 10;
      pointer-events: none;
    }
    .overlay.show {
      display: flex;
      pointer-events: auto;
    }
    .overlay h2 {
      color: #fff;
      letter-spacing: 4px;
      text-shadow: 0 0 8px rgba(255,255,255,.4);
    }
    .footer {
      margin-top: 12px;
      font-size: 12px;
      opacity: .6;
      text-align: center;
    }
    /* Glitch */
    .glitch {
      animation: glitch .25s 2;
    }
    @keyframes glitch {
      0% { transform: translate(0) }
      25% { transform: translate(-2px,1px) }
      50% { transform: translate(2px,-1px) }
      75% { transform: translate(-1px,2px) }
      100% { transform: translate(0) }
    }
    /* Скример-изображение */
    .screamer {
      position: fixed;
      inset: 0;
      background: black;
      display: flex;
      align-items: center;
      justify-content: center;
      z-index: 9999;
      pointer-events: none;
      opacity: 0;
      transition: opacity .1s ease-in;
    }
    .screamer.active {
      opacity: 1;
      pointer-events: auto;
    }
    .screamer img {
      max-width: 100%;
      max-height: 100%;
      object-fit: contain;
      filter: brightness(1.3) contrast(1.5) saturate(1.8);
    }
    /* Эффект "шум" на экране */
    .static {
      position: fixed;
      inset: 0;
      background-image:
        url('data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAADIAAABGCAYAAADK959HAAAAAXNSR0IArs4c6QAAAARnQU1BAACxjwv8YQUAAAAJcEhZcwAADsMAAA7DAcdvqGQAAABWSURBVHhe7dExAQAAAMCg9U9tDXcDAAAAwB8A/wE6q7gB+gAAAABJRU5ErkJggg==');
      opacity: 0;
      z-index: 8;
      pointer-events: none;
      animation: static-noise 0.2s infinite linear;
    }
    @keyframes static-noise {
      0%, 100% { opacity: 0.05; }
      50% { opacity: 0.15; }
    }
  </style>
</head>
<body>
  <!-- Скримеры (скрыты по умолчанию) -->
  <div class="screamer" id="screamer1">
    <img src="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 200 200'%3E%3Cpath fill='red' d='M100,30 C60,30 40,70 70,90 C100,110 130,90 160,90 C190,70 170,30 130,30 Z'/%3E%3Ccircle fill='black' cx='80' cy='70' r='10'/%3E%3Ccircle fill='black' cx='120' cy='70' r='10'/%3E%3C/svg%3E" alt="Ужасное лицо">
  </div>
  <div class="screamer" id="scream
